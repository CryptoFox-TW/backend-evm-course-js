const ResponseFormatterMiddleware = require('./response-formatter-middleware');
const RequestLoggerMiddleware = require('./request-logger-middleware');

module.exports = {
  ResponseFormatterMiddleware,
  RequestLoggerMiddleware,
};
